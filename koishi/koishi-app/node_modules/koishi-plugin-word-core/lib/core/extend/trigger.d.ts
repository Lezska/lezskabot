export declare const trigger: triggerType;
/**
 * 新增trigger
 * @param triggerName 此触发器的id
 * @param replaceStr 在问中以此字符取代正则所匹配的内容
 * @param matchReg 此内容的正则
 * @returns
 */
export declare const addTrigger: (triggerName: string, replaceStr: string, matchReg: string) => triggerType;
/**
 * 删除trigger
 * @param replaceStr 在问中以此字符取代正则所匹配的内容
 * @param matchReg 此内容的正则
 * @returns
 */
export declare const rmTrigger: (replaceStr: string, matchReg: string) => triggerType;
export type triggerType = {
    [key: string]: {
        reg: string[];
        id: string;
    };
};
export interface triggerFunction {
    trigger: triggerType;
    addTrigger: (triggerName: string, replaceStr: string, matchReg: string) => triggerType;
    rmTrigger: (replaceStr: string, matchReg: string) => triggerType;
}
