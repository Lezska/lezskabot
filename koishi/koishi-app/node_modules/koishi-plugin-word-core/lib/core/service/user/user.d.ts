import { readDBType, writeDBType } from "../index";
import { settingType, settingTypeValue } from "../../index";
export declare class User {
    private readDBTools;
    private writeDBTools;
    constructor(readDBTools: readDBType, writeDBTools: writeDBType);
    /**
     * 获取用户背包数据
     * @param uid
     * @returns 背包数据
     */
    getData(uid: string): Promise<Record<string, Record<string, number | string[]>>>;
    /**
     * 保存数据为用户数据
     * @param uid 用户id
     * @param data 完整用户数据
     * @returns
     */
    updateData(uid: string, data: Record<string, Record<string, number | string[]>>): Promise<boolean>;
    /**
     * 获取用户背包数据
     * @param uid 用户id
     * @param cell 存储格
     * @param itemName 物品名称
     * @returns 数量
     */
    getItem(uid: string, cell: string, itemName: string): Promise<null | number | string[]>;
    /**
     * 强制保存物品数量(一般不使用)
     * @param uid 用户id
     * @param cell 存储格
     * @param itemName 物品名称
     * @param itemData 数量
     * @returns
     */
    updateItem(uid: string, cell: string, itemName: string, itemData: number | string[]): Promise<boolean>;
    /**
     * 获取用户编辑指针
     * @param uid 用户id
     * @returns
     */
    getEditWord(uid: string): Promise<string>;
    /**
     * 修改编辑目标
     * @param uid 用户id
     * @param newDB 编辑目标
     * @returns
     */
    setEditWord(uid: string, newDB: string): Promise<boolean>;
    private configTempData;
    /**
     * 在配置中读取设置
     * @param uid
     * @returns
     */
    getConfig(uid: string): Promise<settingType>;
    setConfig(uid: string, key: string, value: settingTypeValue): Promise<void>;
    setConfigForce(uid: string, key: string, value: settingTypeValue): Promise<void>;
    saveConfig(): Promise<boolean>;
    private saveUserConfig;
}
