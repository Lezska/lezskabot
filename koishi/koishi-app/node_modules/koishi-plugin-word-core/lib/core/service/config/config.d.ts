import { settingTypeValue } from '../..';
import * as wordTools from '../index';
export declare class Config {
    private readDB;
    private writeDB;
    private getDB;
    private removeDB;
    private addCache;
    private rmCache;
    private refreshCache;
    constructor(tools: wordTools.ToolsFunction, cache: wordTools.CacheFunction);
    /**
     * 获取配置
     * @param key 配置的键
     * @returns
     */
    getConfig(key: string): Promise<settingTypeValue>;
    /**
     * 更新(新建)配置
     * @param key 配置的键
     * @param value 配置的值
     * @returns
     */
    updateConfig(key: string, value: settingTypeValue): Promise<boolean>;
}
