import * as wordTools from '../index';
export declare class Permission {
    private readDB;
    private writeDB;
    constructor(readDB: wordTools.readDBType, writeDB: wordTools.writeDBType);
    /**
     * 为某人添加权限
     * @param uid 被操作者uid
     * @param newPermission 权限树
     * @returns
     */
    add(uid: string, newPermission: string): Promise<boolean | "已存在此权限">;
    /**
     * 删除某人的权限
     * @param uid 被操作者uid
     * @param permission 权限树
     * @returns
     */
    rm(uid: string, permission: string): Promise<boolean | "不存在此权限">;
    /**
     * 判断某人是否拥有某权限
     * @param uid 查询目标uid
     * @param permission 权限树
     * @returns
     */
    isHave(uid: string, permission: string): Promise<boolean>;
    /**
     * 查看某人的全部权限
     * @param uid 被操作者uid
     * @returns 权限树
     */
    all(uid: string): Promise<string[]>;
}
