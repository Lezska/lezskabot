import { Context, Bot, Fragment } from "koishi";
import { SendOptions } from "@satorijs/protocol";
export declare class wordBot<C extends Context> extends Bot<C> {
    constructor(ctx: C, config: wordBot.Config);
    createSession(msg: string): void;
    sendMessage(channelId: string, content: Fragment, guildId?: string, options?: SendOptions): Promise<string[]>;
    sendPrivateMessage(userId: string, content: Fragment, guildId?: string, options?: SendOptions): Promise<string[]>;
}
export declare namespace wordBot {
    interface Config {
    }
}
export default wordBot;
