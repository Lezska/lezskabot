import { type Context, h } from "koishi";
/**
 * 将options的参数转换为drawBanGDreamBorder的配置
 * @param options
 * @return 适用于边框绘制的参数
 */
export declare function handleBanGDreamConfig(options: any): Promise<{
    color: string;
    band: string;
    starType: string;
    starNum: number;
    border: string;
}>;
/**
 * "封印"绘图功能
 * @param ctx Context
 * @param baseImage 被封印的图片url
 * @return 画完的图片 h对象
 */
export declare function drawLock(ctx: Context, baseImage: string): Promise<"发生错误" | "输入无效" | h>;
/**
 * "卖掉了"绘图函数
 * @param ctx
 * @param baseImage
 * @return 画完的图片 h对象
 */
export declare function drawSold(ctx: Context, baseImage: string): Promise<"发生错误" | "输入无效" | h>;
/**
 * BanG Dream!边框绘制功能
 * @param ctx Koishi上下文
 * @param avatar 要绘制的底图
 * @param inputOptions 输入参数，接受color band starType starNum border
 */
export declare function drawBanGDream(ctx: Context, avatar: string, inputOptions?: {
    color: string;
    band: string;
    starType: string;
    starNum: number;
    border: string;
}): Promise<string>;
