import { type Context, type Session } from "koishi";
import { type Config } from "../index";
interface CurrencyCode {
    country: string;
    currencyCn: string;
    code: string;
}
export declare function intervalGetExchangeRate(_ctx: Context, _cfg: Config, _session: Session, _searchString: string, _exchangeRatePath: string): Promise<void>;
export declare function getExchangeRate(ctx: Context, cfg: Config, session: Session, searchString?: string, raw?: string, retryTimes?: number): Promise<void>;
/**
 * 绘制走势图
 * @param ctx koishi上下文
 * @param prices 收盘价数组
 * @param timeStamps ISO 时间戳数组
 * @param title 图表标题
 * @param width 图表宽度
 * @param height 图表高度
 * @returns buffer
 */
export declare function drawCurrencyChartSkia(ctx: Context, prices: number[], timeStamps: string[], title?: string, width?: number, height?: number): Promise<Buffer>;
export declare function getCurrencyCodes(): Promise<CurrencyCode[]>;
export declare function getCurrencyCodesMap(): Promise<Record<string, string>>;
export {};
