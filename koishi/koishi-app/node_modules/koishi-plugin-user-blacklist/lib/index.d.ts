import { Context, Schema } from 'koishi';
export interface BlacklistConfig {
    enabled: boolean;
    blacklistedUsers: string[];
    replyText: string;
    replyImageUrl: string;
    sendBoth: boolean;
}
export declare const Config: Schema<BlacklistConfig>;
export declare function apply(ctx: Context, config: BlacklistConfig): void;
