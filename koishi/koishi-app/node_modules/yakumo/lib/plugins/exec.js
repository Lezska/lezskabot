export const inject = ['yakumo'];
export function apply(ctx) {
    ctx.register('exec', async () => {
        const { argv } = ctx.yakumo;
        const paths = ctx.yakumo.locate(argv._, {
            filter: () => true,
        });
        console.log(paths);
    });
}
//# sourceMappingURL=exec.js.map