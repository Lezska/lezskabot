import { Context, Schema, Session } from 'koishi';
export declare const name = "rizgame-guess-letters";
export interface Config {
    randSongMode: string;
    startValue?: number;
    endValue?: number;
    value?: number;
}
export declare const Config: Schema<Config>;
declare module 'koishi' {
    interface Channel {
        letterGameStatus: {
            isInGame: boolean;
            gameStatus: {
                guessedLetters?: string[];
                songs: string[];
                guessedSids?: number[];
            };
        };
    }
    interface Tables {
        songlist: SongList;
    }
}
export interface SongList {
    id: number;
    name: string;
    hardness: number;
}
export declare function outputGuessStatus(sess: Session, stat: any): Promise<void>;
export declare function apply(ctx: Context, cfg: Config): Promise<void>;
