/**
 * 文件存储管理器
 * @description 提供文件读写、缓存管理、路径解析等功能
 */
export declare class FileStore {
    private readonly baseDir;
    private readonly cacheDir;
    /**
     * 创建文件存储实例
     * @param rootDir 根目录路径
     */
    constructor(rootDir: string);
    /**
     * 构建文件路径
     * @param type 文件类型
     * @param locale 可选的语言代码
     * @returns 完整文件路径
     */
    private makePath;
    /**
     * 检查文件是否存在
     * @param type 文件类型
     * @param locale 可选的语言代码
     * @returns 文件是否存在
     */
    exists(type: string, locale?: string): Promise<boolean>;
    /**
     * 保存数据到文件
     * @param type 文件类型
     * @param data 要保存的数据
     * @param locale 可选的语言代码
     */
    save<T>(type: string, data: T, locale?: string): Promise<void>;
    /**
     * 从文件读取数据
     * @param type 文件类型
     * @param locale 可选的语言代码
     * @returns 读取的数据或null
     */
    load<T>(type: string, locale?: string): Promise<T | null>;
    /**
     * 解析资源路径
     * @param path 输入路径（URL或文件名）
     * @returns 处理后的路径
     */
    resolve(path: string): string;
    /**
     * 检查本地资源文件是否存在
     * @param filename 文件名
     * @returns 是否存在
     */
    check(filename: string): Promise<boolean>;
    /**
     * 生成缓存键
     * @param commands 指令列表
     * @param config 配置对象
     * @param cmdName 可选的指令名称
     * @returns 缓存键字符串
     */
    generateCacheKey(commands: any[], config: any, cmdName?: string): string;
    /**
     * 生成配置哈希
     * @param config 配置对象
     * @returns 配置哈希字符串
     */
    private hashConfig;
    /**
     * 生成指令列表哈希
     * @param commands 指令列表
     * @returns 指令哈希字符串
     */
    private hashCommands;
    /**
     * 获取缓存文件内容
     * @param key 缓存键
     * @returns 缓存文件的Buffer数据，如果不存在则返回null
     */
    getCache(key: string, ext?: string): Promise<Buffer | null>;
    /**
     * 保存数据到缓存文件
     * @param key 缓存键
     * @param buffer 要保存的Buffer数据
     */
    saveCache(key: string, buffer: Buffer, ext?: string): Promise<void>;
    /**
     * 清除缓存文件
     * @param cmdName 可选的指令名称，用于清除特定指令的缓存
     */
    clearCache(cmdName?: string): Promise<void>;
}
