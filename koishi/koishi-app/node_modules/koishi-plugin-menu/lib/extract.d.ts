import { Context } from 'koishi';
/**
 * 指令选项接口
 */
interface Option {
    name: string;
    desc: string;
    syntax: string;
    hidden: boolean;
    authority: number;
}
/**
 * 指令名称项接口
 */
interface NameItem {
    name: string;
    enabled: boolean;
    isDefault?: boolean;
}
/**
 * 指令接口
 */
export interface Command {
    group: string;
    name: NameItem[];
    hidden: boolean;
    authority: number;
    desc: string;
    usage: string;
    examples: string;
    options: Option[];
    subs?: Command[];
}
/**
 * 指令提取器
 * @description 负责从Koishi上下文中提取和处理指令信息
 */
export declare class Extract {
    private readonly ctx;
    constructor(ctx: Context);
    /**
     * 获取会话语言代码
     * @param session 会话对象
     * @returns 语言代码
     */
    locale(session: any): string;
    /**
     * 获取所有可用指令
     * @param session 会话对象
     * @param locale 语言代码
     * @returns 指令列表
     */
    all(session: any, locale?: string): Promise<Command[]>;
    /**
     * 获取单个指令信息
     * @param session 会话对象
     * @param cmdName 指令名称
     * @param locale 语言代码
     * @returns 指令对象或null
     */
    single(session: any, cmdName: string, locale?: string): Promise<Command | null>;
    /**
     * 获取相关指令列表
     * @param session 会话对象
     * @param cmdName 指令名称
     * @param locale 语言代码
     * @returns 相关指令列表
     */
    related(session: any, cmdName: string, locale?: string): Promise<Command[]>;
    /**
     * 过滤指令列表
     * @param commands 指令列表
     * @param showHidden 是否显示隐藏项
     * @param isDetailView 是否为详情视图
     * @returns 过滤后的指令列表
     */
    filter(commands: Command[], showHidden?: boolean, isDetailView?: boolean): Command[];
    /**
     * 设置会话语言
     * @param session 会话对象
     * @param locale 语言代码
     */
    private setLocale;
    /**
     * 查找指令
     * @param target 目标指令名
     * @param session 会话对象
     * @returns 指令对象或快捷方式列表
     */
    private find;
    /**
     * 处理指令名称配置
     * @param command 指令对象
     * @returns 名称配置数组
     */
    private processNameConfig;
    /**
     * 获取有效数据
     * @param command 指令对象
     * @returns 有效的配置数据
     */
    private getEffectiveData;
    /**
     * 获取指令权限等级
     * @param command 指令对象
     * @returns 权限等级
     */
    private getAuthority;
    /**
     * 获取有效文本内容
     * @param texts 文本配置
     * @param path 文本路径
     * @param session 会话对象
     * @param fallback 回退函数
     * @returns 文本内容
     */
    private getEffectiveText;
    /**
     * 构建指令对象
     * @param command 原始指令对象
     * @param session 会话对象
     * @param userAuth 用户权限
     * @returns 构建的指令对象或null
     */
    private build;
    /**
     * 清理文本内容
     * @param data 原始数据
     * @returns 清理后的文本
     */
    private cleanText;
    /**
     * 获取使用方法文本
     * @param command 指令对象
     * @param session 会话对象
     * @param name 指令名称
     * @param texts 文本配置
     * @returns 使用方法文本
     */
    private getUsageText;
    /**
     * 获取示例文本
     * @param command 指令对象
     * @param getText 获取文本函数
     * @param name 指令名称
     * @returns 示例文本
     */
    private getExamplesText;
    /**
     * 构建选项列表
     * @param effectiveOptions 有效选项配置
     * @param session 会话对象
     * @param userAuth 用户权限
     * @param getText 获取文本函数
     * @param cmdName 指令名称
     * @param texts 文本配置
     * @returns 选项列表
     */
    private buildOptions;
    /**
     * 构建子指令列表
     * @param command 指令对象
     * @param session 会话对象
     * @param userAuth 用户权限
     * @returns 子指令列表或undefined
     */
    private buildSubCommands;
}
export {};
