import { Command } from './extract';
/**
 * 主题渲染器
 * @description 负责生成HTML内容和CSS样式
 */
export declare class Render {
    /**
     * 构建完整的HTML页面
     * @param config 渲染配置
     * @param commands 指令列表
     * @param cmdName 可选的指令名称
     * @returns 完整的HTML字符串
     */
    build(config: any, commands: Command[], currentPage?: number, pageSize?: number, cmdName?: string): string;
    /**
     * 生成CSS样式
     * @param config 渲染配置
     * @returns CSS样式字符串
     */
    private buildCSS;
    /**
     * 构建页面内容
     * @param config 渲染配置
     * @param commands 指令列表
     * @param cmdName 可选的指令名称
     * @returns HTML内容字符串
     */
    private buildContent;
    /**
     * 构建指令列表视图
     * @param commands 指令列表
     * @returns HTML字符串
     */
    private buildList;
    /**
     * 构建指令详情视图
     * @param commands 指令列表
     * @param cmdName 指令名称
     * @returns HTML字符串
     */
    private buildDetail;
    /**
     * 构建详情卡片
     * @param title 卡片标题
     * @param content 卡片内容
     * @returns HTML字符串
     */
    private buildDetailCard;
}
