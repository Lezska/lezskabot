import { Context, Service, Schema } from "koishi";
export declare const name = "QhzyNode";
export interface Config {
    storeDir: string;
    proxyPrefix: string;
}
export declare const Config: Schema<Config>;
declare module 'koishi' {
    interface Context {
        node: QhzyNode;
    }
}
export declare class QhzyNode extends Service {
    constructor(ctx: Context, config: Config);
    protected start(): Promise<void>;
    /**
     * 执行跨平台命令的函数
     * @param command 要执行的命令
     * @param args 执行命令的参数
     * @param cwd 执行命令的工作目录
     * @returns 执行结果的 Promise
     */
    runCommand(command: string, args?: string[], cwd?: string): Promise<string>;
    safeImport<T>(packageName: string): Promise<T>;
}
export declare function apply(ctx: Context): void;
