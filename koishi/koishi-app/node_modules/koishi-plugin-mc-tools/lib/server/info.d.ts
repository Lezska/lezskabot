import { Config } from '../index';
/**
 * 向 Koishi 注册 `mc.info` 和 `mc.info.be` 命令。
 * @param {any} parent - 父命令对象。
 * @param {Config} config - 插件配置。
 */
export declare function registerInfo(parent: any, config: Config): void;
